const typeKey = '__type';

const userClassName = '_User';
const installationClassName = '_Installation';

const idKey = 'objectId';
const createdAtKey = 'createdAt';
const userNameKey = 'username';
const sessionTokenKey = 'sessionToken';
const deviceTokenKey = 'deviceToken';
